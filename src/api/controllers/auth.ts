import { Request, Response } from "express";
import { catchAsync } from "src/utils";

export const signUpController = catchAsync((req: Request, res: Response) => {});

export const loginController = catchAsync((req: Request, res: Response) => {});
